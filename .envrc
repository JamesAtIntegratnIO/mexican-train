# direnv: drop into the flake shell automatically.
#   direnv allow
use flake

# Local, uncommitted secrets. Only one is needed for day-to-day work:
#   export CLOUDFLARE_API_TOKEN="..."
# The R2 credentials for Terraform state live in terraform/backend.hcl instead.
[ -f .env.local ] && dotenv .env.local
