node_modules
npm-debug.log
.git
.gitignore
.claude
.env
.env.*
*.log
.DS_Store
README.md
fly.toml
.wrangler
wrangler.toml
flake.nix
flake.lock
terraform
dist

# worker/ is deliberately NOT ignored: the Node image never runs it, but the
# builder type-checks the tests, and those exercise the Durable Object. It is
# two files, and the alternative is a second tsconfig that makes the image
# build differ from the one CI runs.
.direnv
